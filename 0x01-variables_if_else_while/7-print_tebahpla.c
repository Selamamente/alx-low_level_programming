#include <stdio.h>
/**
* main - prints the lowwer alphabets in reverse
* Description: smile in the mirror
* Return: return 0 (success)
*/
int main(void)
{
int n;
for (n = 'z'; n >= 'a'; n--)
{
putchar(n);
}
putchar(10);
return (0);
}
