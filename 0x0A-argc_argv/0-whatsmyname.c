#include <stdio.h>

/**
* main - prints a fuctionn its name
* @argc: argc parameter
* @argv: an array of a command list
* Return: 0 for succes
*
*/

int main(int argc __attribute__((unused)), char *argv[])
{

printf("%s\n", *argv);
return (0);

}
