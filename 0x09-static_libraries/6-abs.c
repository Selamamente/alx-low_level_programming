#include "main.h"

/**
* _abs - Write a function that computes the absolute value of an integer
* @n: parameter used in the if clause
* Return: value of n
*
*/

int _abs(int n)
{

if (n < 0)
	n = -n;
return (n);
}
