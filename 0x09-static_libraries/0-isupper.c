#include "main.h"

/**
* _isupper - checks for uppercase character
* @c: isupper
* Return: Returns 1 if c is uppercase otherwise Returns 0
*
*/

int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
