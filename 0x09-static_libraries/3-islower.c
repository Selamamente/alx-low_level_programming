#include "main.h"

/**
* _islower - Write a function that checks for lowercase character
* @c: a character in islower
* Return: returns 1 if c is lowercase otherwise returns 0
*
*/

int _islower(int c)
{

return (c >= 'a' && c <= 'z');
}
